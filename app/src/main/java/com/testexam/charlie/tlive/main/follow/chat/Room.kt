package com.testexam.charlie.tlive.main.follow.chat

//targetEmail, targetName, currentChat, badgeCount
data class Room(val targetEmail : String,
                val targetName : String,
                val currentChat : String,
                val badgeCount : Int
                )

